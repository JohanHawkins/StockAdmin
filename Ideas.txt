# Sistema de Gestión de Inventario Web

## Cuentas de Prueba

| Rol | Email | Contraseña |
|-----|-------|------------|
| Admin | admin@admin.com | 123456 |
| Usuario | user@user.com | 123456 |

---

## Objetivo del Proyecto

Construir una aplicación web moderna para administrar inventario de manera sencilla y profesional.

---

# Tecnologías

- **Frontend:** Angular
- **Backend:** ASP.NET Core (futuro)
- **Base de Datos:** PostgreSQL

---

# Permisos por Rol

| Funcionalidad | Admin | Usuario |
|---------------|-------|---------|
| Dashboard | CRUD completo | Solo lectura |
| Productos | Crear, Editar, Eliminar, Activar/Desactivar | Solo lectura |
| Categorías | Crear, Editar, Eliminar | Solo lectura |
| Movimientos | Crear, Filtros | Crear, Filtros |

---

# Estado del Proyecto

## Completado (91%)

### Login ✅
- Formulario de login (email + contraseña)
- Servicio de autenticación con roles
- Guard de rutas protegidas
- Redirección al dashboard tras login

### Dashboard ✅
- Total de productos, categorías, movimientos
- Stock total y valor del inventario
- Producto con mayor stock y más costoso
- Último movimiento registrado
- Tablas de productos recientes, stock bajo y movimientos

### Productos ✅
- CRUD completo (crear, editar, eliminar)
- Código automático (P001, P002...)
- Búsqueda por nombre
- Cambiar estado (Activar/Desactivar)
- Campos: código, nombre, descripción, precio, stock, stock mínimo, categoría, estado
- Validaciones completas
- Formato de moneda con separadores

### Categorías ✅
- CRUD completo
- Código automático (C001, C002...)
- Evitar eliminar categoría con productos asociados
- Mostrar cantidad de productos por categoría

### Movimientos ✅
- Registrar entradas y salidas
- Actualización automática del stock
- Validación de stock para salidas
- Filtros por fecha, tipo y producto
- Campo de observación

### UI/UX ✅
- Layout con sidebar colapsable y navbar
- Modo oscuro con toggle y persistencia
- Responsive (móvil)
- Variables CSS globales
- Animaciones y transiciones
- Login con efecto glassmorphism
- Loading states y manejo de errores
- Página 404

### Modelos de Datos ✅
- Product: code, name, description, price, stock, minStock, categoryCode, status
- Category: code, name
- Movement: id, productCode, type, quantity, date, observation
- User: id, nombre, email, password, role

---

## Pendientes

| # | Tarea | Módulo |
|---|-------|--------|
| 1 | Exportar productos a CSV | Productos |
| 2 | Importar productos desde CSV | Productos |
| 3 | Filtros avanzados de productos | Productos |
| 4 | Exportar historial de movimientos | Movimientos |
| 5 | Pruebas unitarias | Infraestructura |
| 6 | Pruebas de integración | Infraestructura |
| 7 | README profesional con imágenes | Infraestructura |
| 8 | Deploy (GitHub Pages / Vercel) | Infraestructura |

---

# Flujo del Sistema

1. Usuario inicia sesión (admin o usuario)
2. Ingresa al dashboard según su rol
3. Administra productos (según permisos)
4. Registra entradas o salidas
5. El sistema actualiza el stock automáticamente
6. Visualiza estadísticas en el dashboard

---

# Base de Datos - PostgreSQL

## Diagrama de Relaciones

```
users (1) ──────< movements (N)
                    │
categories (1) ───< products (N) >──── movements (N)
```

## Tablas

### users
Almacena los usuarios del sistema.

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    email VARCHAR(150) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    role VARCHAR(10) NOT NULL CHECK (role IN ('admin', 'user')),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### categories
Almacena las categorías de productos.

```sql
CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    code VARCHAR(10) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### products
Almacena los productos del inventario.

```sql
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    code VARCHAR(10) UNIQUE NOT NULL,
    name VARCHAR(150) NOT NULL,
    description TEXT,
    price DECIMAL(10, 2) NOT NULL CHECK (price > 0),
    stock INTEGER NOT NULL DEFAULT 0 CHECK (stock >= 0),
    min_stock INTEGER NOT NULL DEFAULT 0 CHECK (min_stock >= 0),
    category_id INTEGER REFERENCES categories(id) ON DELETE RESTRICT,
    status VARCHAR(10) NOT NULL DEFAULT 'Activo' CHECK (status IN ('Activo', 'Inactivo')),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### movements
Almacena los movimientos de inventario (entradas/salidas).

```sql
CREATE TABLE movements (
    id SERIAL PRIMARY KEY,
    code VARCHAR(10) UNIQUE NOT NULL,
    product_id INTEGER REFERENCES products(id) ON DELETE RESTRICT,
    type VARCHAR(10) NOT NULL CHECK (type IN ('ENTRADA', 'SALIDA')),
    quantity INTEGER NOT NULL CHECK (quantity > 0),
    observation TEXT,
    user_id INTEGER REFERENCES users(id) ON DELETE RESTRICT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## Relaciones

| Relación | Tipo | Descripción |
|----------|------|-------------|
| users → movements | 1:N | Un usuario puede registrar muchos movimientos |
| categories → products | 1:N | Una categoría puede tener muchos productos |
| products → movements | 1:N | Un producto puede tener muchos movimientos |

## Índices Recomendados

```sql
-- Búsquedas frecuentes
CREATE INDEX idx_products_category ON products(category_id);
CREATE INDEX idx_products_status ON products(status);
CREATE INDEX idx_products_code ON products(code);
CREATE INDEX idx_movements_product ON movements(product_id);
CREATE INDEX idx_movements_user ON movements(user_id);
CREATE INDEX idx_movements_type ON movements(type);
CREATE INDEX idx_movements_created ON movements(created_at);
CREATE INDEX idx_users_email ON users(email);
```

## Datos Iniciales (Seed)

```sql
-- Usuario admin por defecto
INSERT INTO users (nombre, email, password, role) 
VALUES ('Administrador', 'admin@admin.com', '123456', 'admin');

-- Usuario regular por defecto
INSERT INTO users (nombre, email, password, role) 
VALUES ('Usuario', 'user@user.com', '123456', 'user');

-- Categorías iniciales
INSERT INTO categories (code, name) VALUES ('C001', 'Tecnología');
INSERT INTO categories (code, name) VALUES ('C002', 'Accesorios');
```
